{% extends 'layout.twig' %}

{% block body %}
 <div class="container mt-3">
    <h1>Parooli muutmine</h1>
<form method = "POST" action ="{{ path_for('cPassword', {'id': user.id}) }}">
  <div class="form-group row">
    <div class="col-4">
      <input type="password" name="password" class="form-control" placeholder="Uus parool">
      <input type="password" name="newpassword" class="form-control" placeholder="Kinnita uus parool">
    </div>
  </div>
  <div class="form-group row">
    <div class="col-sm-10">
      <button  type="submit" name="submit2" class="btn btn-primary">Muuda</button>
    </div>
  </div>
</form>
</div>
 
{% endblock %}
