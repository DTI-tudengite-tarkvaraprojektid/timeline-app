{% extends 'layout.twig' %}

{% block body %}
  <div class="container mt-3">
      <div class="row my-4">
          <div class="col-auto">
            <h3>Failid</h3>
          </div>
        <div class="col text-right">
          <form id="fsearch-form" action="{{ path_for('files') }}">
            <div class="input-group mb-3">
                      <input aria-describedby="basic-addon2" aria-label="" class="form-control" id="fsearch" placeholder="Otsi faili" type="text">
              <div class="input-group-append">
                <button class="btn btn-outline-secondary" id="fsearchbutton" type="submit">Otsi</button>
              </div>
            </div>
          </form>
        </div>
      </div>
    <div class="container">
      {% for group in groups %}
        <h5 class="list-group mb-3">Ajajoone {{ group[0].event.timeline.name }} failid</h5>

          {% for file in group %}
              <a class="list-group-item list-group-item-action d-flex justify-content-between align-items-baseline" href="{{ file.content.path }}" data-caption="Sündmus: {{ file.event.title }}" download="{{ file.content.name }}" class="list-group-item-action">
                <span><i class="fas fa-download mr-2"></i> {{ file.content.name }} </span>
                <span>{{ file.updated_at}}</span>
              </a>
          {% endfor %}
        <hr>
      {% endfor %}
    </div>
  </div>
{% endblock %}
