{% extends 'layout.twig' %}

{% block body %}
 <div class="container mt-3">
    <h1>Konto seaded</h1>
<form method = "POST" action ="{{ path_for('settings') }}">
  <div class="form-group">
    <div class="col-4">
     <label class="col-sm-2 col-form-label">Email</label>
      <input type="text" name="email" class="form-control" value="{{ user.email }}">
    </div>
  </div>

  <div class="form-group">
    <div class="col-4">
      <label class="col-sm-2 col-form-label">Eesnimi</label>
      <input type="text" name="firstname"class="form-control" value="{{ user.firstname }}">
    </div>
     </div>

    <div class="form-group">
     <div class="col-4">
     <label class="col-sm-2 col-form-label">Perekonnanimi</label>
         <input type="text" name="lastname" class="form-control" value="{{ user.lastname }}">
     </div>
    </div>

  <div class="form-group">
    <div class="col-sm-10">
      <button  type="submit" name="submit" class="btn btn-primary">Submit</button>
    </div>
  </div>
  <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path_for('cPassword', {'id': user.id}) }}">muuda parooli</a>
            </li>
        </ul>
</form>
</div>

{% endblock %}
